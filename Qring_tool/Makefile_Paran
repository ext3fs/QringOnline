
.SUFFIXES : .c .o

CC = gcc

INCL	= -I/usr/local/include/mysql/include/mysql
LIBS	= -L/usr/local/mysql/lib/mysql -lmysqlclient -lz

OBJS	= main.o

TARGET	= test

.c.o	:
	$(CC) -c -Wall $< $(INCL)

$(TARGET) : $(OBJS)
	$(CC) -g -o $(TARGET) $(OBJS) $(LIBS) 

clean :
	rm -rf $(OBJS) $(TARGET) core

new :
	$(MAKE) clean
	$(MAKE)

